'make clean', 'make' work as expected

flags:\
  -o, --output [filename] (saves song as a wav file to this file)\
  -s, --seed [int] (seed for randomness)\
  -b, --base (generates with base if given this flag)

run with './aleatoric'
